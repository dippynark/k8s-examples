# OAuth 2

```sh
kubectl get namespace desktop || kubectl create namespace desktop
helm upgrade --install desktop -n desktop .

vglrun glxinfo | grep -i opengl
vglrun /opt/VirtualGL/bin/glxspheres64

xdpyinfo -display :0
xdpyinfo -display :1
```
